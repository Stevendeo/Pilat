int main(){
  int x, y, a, b, p, q, r, s;
  while (a != b) {
   	if (a>b) {a = a-b; p = p-q; r=r-s;}

        else {b = b-a; q = q-p; s = s-r;}
      }
    }
}
/* CAV96 - 

SPL <- logic temp
Livre concur ++ : Jean Michel Ranyal (Springer)

*/
